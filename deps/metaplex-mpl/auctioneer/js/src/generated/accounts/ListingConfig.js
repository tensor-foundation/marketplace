"use strict";
/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.listingConfigBeet = exports.ListingConfig = void 0;
const beet = __importStar(require("@metaplex-foundation/beet"));
const ListingConfigVersion_1 = require("../types/ListingConfigVersion");
const Bid_1 = require("../types/Bid");
const listingConfigDiscriminator = [183, 196, 26, 41, 131, 46, 184, 115];
/**
 * Holds the data for the {@link ListingConfig} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
class ListingConfig {
    constructor(version, startTime, endTime, highestBid, bump, reservePrice, minBidIncrement, timeExtPeriod, timeExtDelta, allowHighBidCancel) {
        this.version = version;
        this.startTime = startTime;
        this.endTime = endTime;
        this.highestBid = highestBid;
        this.bump = bump;
        this.reservePrice = reservePrice;
        this.minBidIncrement = minBidIncrement;
        this.timeExtPeriod = timeExtPeriod;
        this.timeExtDelta = timeExtDelta;
        this.allowHighBidCancel = allowHighBidCancel;
    }
    /**
     * Creates a {@link ListingConfig} instance from the provided args.
     */
    static fromArgs(args) {
        return new ListingConfig(args.version, args.startTime, args.endTime, args.highestBid, args.bump, args.reservePrice, args.minBidIncrement, args.timeExtPeriod, args.timeExtDelta, args.allowHighBidCancel);
    }
    /**
     * Deserializes the {@link ListingConfig} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static fromAccountInfo(accountInfo, offset = 0) {
        return ListingConfig.deserialize(accountInfo.data, offset);
    }
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link ListingConfig} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    static fromAccountAddress(connection, address) {
        return __awaiter(this, void 0, void 0, function* () {
            const accountInfo = yield connection.getAccountInfo(address);
            if (accountInfo == null) {
                throw new Error(`Unable to find ListingConfig account at ${address}`);
            }
            return ListingConfig.fromAccountInfo(accountInfo, 0)[0];
        });
    }
    /**
     * Deserializes the {@link ListingConfig} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static deserialize(buf, offset = 0) {
        return exports.listingConfigBeet.deserialize(buf, offset);
    }
    /**
     * Serializes the {@link ListingConfig} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    serialize() {
        return exports.listingConfigBeet.serialize(Object.assign({ accountDiscriminator: listingConfigDiscriminator }, this));
    }
    /**
     * Returns the byteSize of a {@link Buffer} holding the serialized data of
     * {@link ListingConfig}
     */
    static get byteSize() {
        return exports.listingConfigBeet.byteSize;
    }
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link ListingConfig} data from rent
     *
     * @param connection used to retrieve the rent exemption information
     */
    static getMinimumBalanceForRentExemption(connection, commitment) {
        return __awaiter(this, void 0, void 0, function* () {
            return connection.getMinimumBalanceForRentExemption(ListingConfig.byteSize, commitment);
        });
    }
    /**
     * Determines if the provided {@link Buffer} has the correct byte size to
     * hold {@link ListingConfig} data.
     */
    static hasCorrectByteSize(buf, offset = 0) {
        return buf.byteLength - offset === ListingConfig.byteSize;
    }
    /**
     * Returns a readable version of {@link ListingConfig} properties
     * and can be used to convert to JSON and/or logging
     */
    pretty() {
        return {
            version: 'ListingConfigVersion.' + ListingConfigVersion_1.ListingConfigVersion[this.version],
            startTime: this.startTime,
            endTime: this.endTime,
            highestBid: this.highestBid,
            bump: this.bump,
            reservePrice: (() => {
                const x = this.reservePrice;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            minBidIncrement: (() => {
                const x = this.minBidIncrement;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            timeExtPeriod: this.timeExtPeriod,
            timeExtDelta: this.timeExtDelta,
            allowHighBidCancel: this.allowHighBidCancel,
        };
    }
}
exports.ListingConfig = ListingConfig;
/**
 * @category Accounts
 * @category generated
 */
exports.listingConfigBeet = new beet.BeetStruct([
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['version', ListingConfigVersion_1.listingConfigVersionBeet],
    ['startTime', beet.i64],
    ['endTime', beet.i64],
    ['highestBid', Bid_1.bidBeet],
    ['bump', beet.u8],
    ['reservePrice', beet.u64],
    ['minBidIncrement', beet.u64],
    ['timeExtPeriod', beet.u32],
    ['timeExtDelta', beet.u32],
    ['allowHighBidCancel', beet.bool],
], ListingConfig.fromArgs, 'ListingConfig');
