"use strict";
/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sellingResourceBeet = exports.SellingResource = exports.sellingResourceDiscriminator = void 0;
const web3 = __importStar(require("@solana/web3.js"));
const beet = __importStar(require("@metaplex-foundation/beet"));
const beetSolana = __importStar(require("@metaplex-foundation/beet-solana"));
const SellingResourceState_1 = require("../types/SellingResourceState");
exports.sellingResourceDiscriminator = [15, 32, 69, 235, 249, 39, 18, 167];
/**
 * Holds the data for the {@link SellingResource} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
class SellingResource {
    constructor(store, owner, resource, vault, vaultOwner, supply, maxSupply, state) {
        this.store = store;
        this.owner = owner;
        this.resource = resource;
        this.vault = vault;
        this.vaultOwner = vaultOwner;
        this.supply = supply;
        this.maxSupply = maxSupply;
        this.state = state;
    }
    /**
     * Creates a {@link SellingResource} instance from the provided args.
     */
    static fromArgs(args) {
        return new SellingResource(args.store, args.owner, args.resource, args.vault, args.vaultOwner, args.supply, args.maxSupply, args.state);
    }
    /**
     * Deserializes the {@link SellingResource} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static fromAccountInfo(accountInfo, offset = 0) {
        return SellingResource.deserialize(accountInfo.data, offset);
    }
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link SellingResource} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    static fromAccountAddress(connection, address, commitmentOrConfig) {
        return __awaiter(this, void 0, void 0, function* () {
            const accountInfo = yield connection.getAccountInfo(address, commitmentOrConfig);
            if (accountInfo == null) {
                throw new Error(`Unable to find SellingResource account at ${address}`);
            }
            return SellingResource.fromAccountInfo(accountInfo, 0)[0];
        });
    }
    /**
     * Provides a {@link web3.Connection.getProgramAccounts} config builder,
     * to fetch accounts matching filters that can be specified via that builder.
     *
     * @param programId - the program that owns the accounts we are filtering
     */
    static gpaBuilder(programId = new web3.PublicKey('SaLeTjyUa5wXHnGuewUSyJ5JWZaHwz3TxqUntCE9czo')) {
        return beetSolana.GpaBuilder.fromStruct(programId, exports.sellingResourceBeet);
    }
    /**
     * Deserializes the {@link SellingResource} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static deserialize(buf, offset = 0) {
        return exports.sellingResourceBeet.deserialize(buf, offset);
    }
    /**
     * Serializes the {@link SellingResource} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    serialize() {
        return exports.sellingResourceBeet.serialize(Object.assign({ accountDiscriminator: exports.sellingResourceDiscriminator }, this));
    }
    /**
     * Returns the byteSize of a {@link Buffer} holding the serialized data of
     * {@link SellingResource} for the provided args.
     *
     * @param args need to be provided since the byte size for this account
     * depends on them
     */
    static byteSize(args) {
        const instance = SellingResource.fromArgs(args);
        return exports.sellingResourceBeet.toFixedFromValue(Object.assign({ accountDiscriminator: exports.sellingResourceDiscriminator }, instance)).byteSize;
    }
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link SellingResource} data from rent
     *
     * @param args need to be provided since the byte size for this account
     * depends on them
     * @param connection used to retrieve the rent exemption information
     */
    static getMinimumBalanceForRentExemption(args, connection, commitment) {
        return __awaiter(this, void 0, void 0, function* () {
            return connection.getMinimumBalanceForRentExemption(SellingResource.byteSize(args), commitment);
        });
    }
    /**
     * Returns a readable version of {@link SellingResource} properties
     * and can be used to convert to JSON and/or logging
     */
    pretty() {
        return {
            store: this.store.toBase58(),
            owner: this.owner.toBase58(),
            resource: this.resource.toBase58(),
            vault: this.vault.toBase58(),
            vaultOwner: this.vaultOwner.toBase58(),
            supply: (() => {
                const x = this.supply;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            maxSupply: this.maxSupply,
            state: 'SellingResourceState.' + SellingResourceState_1.SellingResourceState[this.state],
        };
    }
}
exports.SellingResource = SellingResource;
/**
 * @category Accounts
 * @category generated
 */
exports.sellingResourceBeet = new beet.FixableBeetStruct([
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['store', beetSolana.publicKey],
    ['owner', beetSolana.publicKey],
    ['resource', beetSolana.publicKey],
    ['vault', beetSolana.publicKey],
    ['vaultOwner', beetSolana.publicKey],
    ['supply', beet.u64],
    ['maxSupply', beet.coption(beet.u64)],
    ['state', SellingResourceState_1.sellingResourceStateBeet],
], SellingResource.fromArgs, 'SellingResource');
