"use strict";
/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.entangledPairBeet = exports.EntangledPair = void 0;
const beet = __importStar(require("@metaplex-foundation/beet"));
const beetSolana = __importStar(require("@metaplex-foundation/beet-solana"));
const entangledPairDiscriminator = [133, 118, 20, 210, 1, 54, 172, 116];
/**
 * Holds the data for the {@link EntangledPair} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
class EntangledPair {
    constructor(treasuryMint, mintA, mintB, tokenAEscrow, tokenBEscrow, authority, bump, tokenAEscrowBump, tokenBEscrowBump, price, paid, paysEveryTime) {
        this.treasuryMint = treasuryMint;
        this.mintA = mintA;
        this.mintB = mintB;
        this.tokenAEscrow = tokenAEscrow;
        this.tokenBEscrow = tokenBEscrow;
        this.authority = authority;
        this.bump = bump;
        this.tokenAEscrowBump = tokenAEscrowBump;
        this.tokenBEscrowBump = tokenBEscrowBump;
        this.price = price;
        this.paid = paid;
        this.paysEveryTime = paysEveryTime;
    }
    /**
     * Creates a {@link EntangledPair} instance from the provided args.
     */
    static fromArgs(args) {
        return new EntangledPair(args.treasuryMint, args.mintA, args.mintB, args.tokenAEscrow, args.tokenBEscrow, args.authority, args.bump, args.tokenAEscrowBump, args.tokenBEscrowBump, args.price, args.paid, args.paysEveryTime);
    }
    /**
     * Deserializes the {@link EntangledPair} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static fromAccountInfo(accountInfo, offset = 0) {
        return EntangledPair.deserialize(accountInfo.data, offset);
    }
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link EntangledPair} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    static fromAccountAddress(connection, address) {
        return __awaiter(this, void 0, void 0, function* () {
            const accountInfo = yield connection.getAccountInfo(address);
            if (accountInfo == null) {
                throw new Error(`Unable to find EntangledPair account at ${address}`);
            }
            return EntangledPair.fromAccountInfo(accountInfo, 0)[0];
        });
    }
    /**
     * Deserializes the {@link EntangledPair} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static deserialize(buf, offset = 0) {
        return exports.entangledPairBeet.deserialize(buf, offset);
    }
    /**
     * Serializes the {@link EntangledPair} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    serialize() {
        return exports.entangledPairBeet.serialize(Object.assign({ accountDiscriminator: entangledPairDiscriminator }, this));
    }
    /**
     * Returns the byteSize of a {@link Buffer} holding the serialized data of
     * {@link EntangledPair}
     */
    static get byteSize() {
        return exports.entangledPairBeet.byteSize;
    }
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link EntangledPair} data from rent
     *
     * @param connection used to retrieve the rent exemption information
     */
    static getMinimumBalanceForRentExemption(connection, commitment) {
        return __awaiter(this, void 0, void 0, function* () {
            return connection.getMinimumBalanceForRentExemption(EntangledPair.byteSize, commitment);
        });
    }
    /**
     * Determines if the provided {@link Buffer} has the correct byte size to
     * hold {@link EntangledPair} data.
     */
    static hasCorrectByteSize(buf, offset = 0) {
        return buf.byteLength - offset === EntangledPair.byteSize;
    }
    /**
     * Returns a readable version of {@link EntangledPair} properties
     * and can be used to convert to JSON and/or logging
     */
    pretty() {
        return {
            treasuryMint: this.treasuryMint.toBase58(),
            mintA: this.mintA.toBase58(),
            mintB: this.mintB.toBase58(),
            tokenAEscrow: this.tokenAEscrow.toBase58(),
            tokenBEscrow: this.tokenBEscrow.toBase58(),
            authority: this.authority.toBase58(),
            bump: this.bump,
            tokenAEscrowBump: this.tokenAEscrowBump,
            tokenBEscrowBump: this.tokenBEscrowBump,
            price: (() => {
                const x = this.price;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            paid: this.paid,
            paysEveryTime: this.paysEveryTime,
        };
    }
}
exports.EntangledPair = EntangledPair;
/**
 * @category Accounts
 * @category generated
 */
exports.entangledPairBeet = new beet.BeetStruct([
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['treasuryMint', beetSolana.publicKey],
    ['mintA', beetSolana.publicKey],
    ['mintB', beetSolana.publicKey],
    ['tokenAEscrow', beetSolana.publicKey],
    ['tokenBEscrow', beetSolana.publicKey],
    ['authority', beetSolana.publicKey],
    ['bump', beet.u8],
    ['tokenAEscrowBump', beet.u8],
    ['tokenBEscrowBump', beet.u8],
    ['price', beet.u64],
    ['paid', beet.bool],
    ['paysEveryTime', beet.bool],
], EntangledPair.fromArgs, 'EntangledPair');
