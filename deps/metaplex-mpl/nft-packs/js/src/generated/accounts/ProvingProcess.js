"use strict";
/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.provingProcessBeet = exports.ProvingProcess = void 0;
const web3 = __importStar(require("@solana/web3.js"));
const beetSolana = __importStar(require("@metaplex-foundation/beet-solana"));
const beet = __importStar(require("@metaplex-foundation/beet"));
const AccountType_1 = require("../types/AccountType");
/**
 * Holds the data for the {@link ProvingProcess} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
class ProvingProcess {
    constructor(accountType, walletKey, isExhausted, voucherMint, packSet, cardsRedeemed, cardsToRedeem) {
        this.accountType = accountType;
        this.walletKey = walletKey;
        this.isExhausted = isExhausted;
        this.voucherMint = voucherMint;
        this.packSet = packSet;
        this.cardsRedeemed = cardsRedeemed;
        this.cardsToRedeem = cardsToRedeem;
    }
    /**
     * Creates a {@link ProvingProcess} instance from the provided args.
     */
    static fromArgs(args) {
        return new ProvingProcess(args.accountType, args.walletKey, args.isExhausted, args.voucherMint, args.packSet, args.cardsRedeemed, args.cardsToRedeem);
    }
    /**
     * Deserializes the {@link ProvingProcess} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static fromAccountInfo(accountInfo, offset = 0) {
        return ProvingProcess.deserialize(accountInfo.data, offset);
    }
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link ProvingProcess} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    static fromAccountAddress(connection, address) {
        return __awaiter(this, void 0, void 0, function* () {
            const accountInfo = yield connection.getAccountInfo(address);
            if (accountInfo == null) {
                throw new Error(`Unable to find ProvingProcess account at ${address}`);
            }
            return ProvingProcess.fromAccountInfo(accountInfo, 0)[0];
        });
    }
    /**
     * Provides a {@link web3.Connection.getProgramAccounts} config builder,
     * to fetch accounts matching filters that can be specified via that builder.
     *
     * @param programId - the program that owns the accounts we are filtering
     */
    static gpaBuilder(programId = new web3.PublicKey('packFeFNZzMfD9aVWL7QbGz1WcU7R9zpf6pvNsw2BLu')) {
        return beetSolana.GpaBuilder.fromStruct(programId, exports.provingProcessBeet);
    }
    /**
     * Deserializes the {@link ProvingProcess} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static deserialize(buf, offset = 0) {
        return exports.provingProcessBeet.deserialize(buf, offset);
    }
    /**
     * Serializes the {@link ProvingProcess} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    serialize() {
        return exports.provingProcessBeet.serialize(this);
    }
    /**
     * Returns the byteSize of a {@link Buffer} holding the serialized data of
     * {@link ProvingProcess} for the provided args.
     *
     * @param args need to be provided since the byte size for this account
     * depends on them
     */
    static byteSize(args) {
        const instance = ProvingProcess.fromArgs(args);
        return exports.provingProcessBeet.toFixedFromValue(instance).byteSize;
    }
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link ProvingProcess} data from rent
     *
     * @param args need to be provided since the byte size for this account
     * depends on them
     * @param connection used to retrieve the rent exemption information
     */
    static getMinimumBalanceForRentExemption(args, connection, commitment) {
        return __awaiter(this, void 0, void 0, function* () {
            return connection.getMinimumBalanceForRentExemption(ProvingProcess.byteSize(args), commitment);
        });
    }
    /**
     * Returns a readable version of {@link ProvingProcess} properties
     * and can be used to convert to JSON and/or logging
     */
    pretty() {
        return {
            accountType: 'AccountType.' + AccountType_1.AccountType[this.accountType],
            walletKey: this.walletKey.toBase58(),
            isExhausted: this.isExhausted,
            voucherMint: this.voucherMint.toBase58(),
            packSet: this.packSet.toBase58(),
            cardsRedeemed: this.cardsRedeemed,
            cardsToRedeem: this.cardsToRedeem,
        };
    }
}
exports.ProvingProcess = ProvingProcess;
/**
 * @category Accounts
 * @category generated
 */
exports.provingProcessBeet = new beet.FixableBeetStruct([
    ['accountType', AccountType_1.accountTypeBeet],
    ['walletKey', beetSolana.publicKey],
    ['isExhausted', beet.bool],
    ['voucherMint', beetSolana.publicKey],
    ['packSet', beetSolana.publicKey],
    ['cardsRedeemed', beet.u32],
    ['cardsToRedeem', beet.map(beet.u32, beet.u32)],
], ProvingProcess.fromArgs, 'ProvingProcess');
