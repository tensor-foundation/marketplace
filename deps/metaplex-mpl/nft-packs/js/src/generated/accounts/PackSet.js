"use strict";
/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.packSetBeet = exports.PackSet = void 0;
const web3 = __importStar(require("@solana/web3.js"));
const beet = __importStar(require("@metaplex-foundation/beet"));
const beetSolana = __importStar(require("@metaplex-foundation/beet-solana"));
const AccountType_1 = require("../types/AccountType");
const PackSetState_1 = require("../types/PackSetState");
const PackDistributionType_1 = require("../types/PackDistributionType");
/**
 * Holds the data for the {@link PackSet} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
class PackSet {
    constructor(accountType, store, authority, description, uri, name /* size: 32 */, packCards, packVouchers, totalWeight, totalEditions, mutable, packState, distributionType, allowedAmountToRedeem, redeemStartDate, redeemEndDate) {
        this.accountType = accountType;
        this.store = store;
        this.authority = authority;
        this.description = description;
        this.uri = uri;
        this.name = name;
        this.packCards = packCards;
        this.packVouchers = packVouchers;
        this.totalWeight = totalWeight;
        this.totalEditions = totalEditions;
        this.mutable = mutable;
        this.packState = packState;
        this.distributionType = distributionType;
        this.allowedAmountToRedeem = allowedAmountToRedeem;
        this.redeemStartDate = redeemStartDate;
        this.redeemEndDate = redeemEndDate;
    }
    /**
     * Creates a {@link PackSet} instance from the provided args.
     */
    static fromArgs(args) {
        return new PackSet(args.accountType, args.store, args.authority, args.description, args.uri, args.name, args.packCards, args.packVouchers, args.totalWeight, args.totalEditions, args.mutable, args.packState, args.distributionType, args.allowedAmountToRedeem, args.redeemStartDate, args.redeemEndDate);
    }
    /**
     * Deserializes the {@link PackSet} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static fromAccountInfo(accountInfo, offset = 0) {
        return PackSet.deserialize(accountInfo.data, offset);
    }
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link PackSet} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    static fromAccountAddress(connection, address) {
        return __awaiter(this, void 0, void 0, function* () {
            const accountInfo = yield connection.getAccountInfo(address);
            if (accountInfo == null) {
                throw new Error(`Unable to find PackSet account at ${address}`);
            }
            return PackSet.fromAccountInfo(accountInfo, 0)[0];
        });
    }
    /**
     * Provides a {@link web3.Connection.getProgramAccounts} config builder,
     * to fetch accounts matching filters that can be specified via that builder.
     *
     * @param programId - the program that owns the accounts we are filtering
     */
    static gpaBuilder(programId = new web3.PublicKey('packFeFNZzMfD9aVWL7QbGz1WcU7R9zpf6pvNsw2BLu')) {
        return beetSolana.GpaBuilder.fromStruct(programId, exports.packSetBeet);
    }
    /**
     * Deserializes the {@link PackSet} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static deserialize(buf, offset = 0) {
        return exports.packSetBeet.deserialize(buf, offset);
    }
    /**
     * Serializes the {@link PackSet} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    serialize() {
        return exports.packSetBeet.serialize(this);
    }
    /**
     * Returns the byteSize of a {@link Buffer} holding the serialized data of
     * {@link PackSet} for the provided args.
     *
     * @param args need to be provided since the byte size for this account
     * depends on them
     */
    static byteSize(args) {
        const instance = PackSet.fromArgs(args);
        return exports.packSetBeet.toFixedFromValue(instance).byteSize;
    }
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link PackSet} data from rent
     *
     * @param args need to be provided since the byte size for this account
     * depends on them
     * @param connection used to retrieve the rent exemption information
     */
    static getMinimumBalanceForRentExemption(args, connection, commitment) {
        return __awaiter(this, void 0, void 0, function* () {
            return connection.getMinimumBalanceForRentExemption(PackSet.byteSize(args), commitment);
        });
    }
    /**
     * Returns a readable version of {@link PackSet} properties
     * and can be used to convert to JSON and/or logging
     */
    pretty() {
        return {
            accountType: 'AccountType.' + AccountType_1.AccountType[this.accountType],
            store: this.store.toBase58(),
            authority: this.authority.toBase58(),
            description: this.description,
            uri: this.uri,
            name: this.name,
            packCards: this.packCards,
            packVouchers: this.packVouchers,
            totalWeight: (() => {
                const x = this.totalWeight;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            totalEditions: (() => {
                const x = this.totalEditions;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            mutable: this.mutable,
            packState: 'PackSetState.' + PackSetState_1.PackSetState[this.packState],
            distributionType: 'PackDistributionType.' + PackDistributionType_1.PackDistributionType[this.distributionType],
            allowedAmountToRedeem: this.allowedAmountToRedeem,
            redeemStartDate: (() => {
                const x = this.redeemStartDate;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            redeemEndDate: this.redeemEndDate,
        };
    }
}
exports.PackSet = PackSet;
/**
 * @category Accounts
 * @category generated
 */
exports.packSetBeet = new beet.FixableBeetStruct([
    ['accountType', AccountType_1.accountTypeBeet],
    ['store', beetSolana.publicKey],
    ['authority', beetSolana.publicKey],
    ['description', beet.utf8String],
    ['uri', beet.utf8String],
    ['name', beet.uniformFixedSizeArray(beet.u8, 32)],
    ['packCards', beet.u32],
    ['packVouchers', beet.u32],
    ['totalWeight', beet.u64],
    ['totalEditions', beet.u64],
    ['mutable', beet.bool],
    ['packState', PackSetState_1.packSetStateBeet],
    ['distributionType', PackDistributionType_1.packDistributionTypeBeet],
    ['allowedAmountToRedeem', beet.u32],
    ['redeemStartDate', beet.u64],
    ['redeemEndDate', beet.coption(beet.u64)],
], PackSet.fromArgs, 'PackSet');
